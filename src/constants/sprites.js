const spriteDir = 'data/sprites';

const SPRITES = {
  backlitButtons: {
    location: `${spriteDir}/backlitButtons.png`,
    config: {
      frameWidth: 32,
      frameHeight: 32,
    },
  },
  modeIndicator: {
    location: `${spriteDir}/modeIndicator.png`,
    config: {
      frameWidth: 40,
      frameHeight: 50,
    },
  },
  regularButtons: {
    location: `${spriteDir}/regularButtons.png`,
    config: {
      frameWidth: 32,
      frameHeight: 32,
    },
  },
  forklift: {
    location: `${spriteDir}/forklift.png`,
    config: {
      frameWidth: 48,
      frameHeight: 32,
    },
  },
};

export default SPRITES;
